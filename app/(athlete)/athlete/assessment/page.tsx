import { PageHeader } from "@/components/app/page-header";

/** Placeholder — replaced by the full Remapping Assessment view (W6). */
export default function AthleteAssessmentPage() {
  return (
    <div className="flex flex-col gap-6">
      <PageHeader
        eyebrow="Athlete Portal · Assessment"
        title="Remapping Assessment"
        description="Your assessment results will appear here."
      />
    </div>
  );
}
